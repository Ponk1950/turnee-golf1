{
  "name": "turnee-golf1",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build"
  },
  "engines": {
    "node": "18.x"
  }
}
The key changes are pinning react-scripts to 5.0.1 (the latest stable version) and telling Netlify to use Node 18 instead of Node 22 — that's what's causing the ajv error.
Commit, then trigger deploy.
